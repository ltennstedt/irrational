---
name: Java CI with Gradle
"on":
  push:
    paths-ignore:
    - .editorconfig
    - .gitignore
    - .gitattributes
    - LICENSE
    - '**/*.md'
  pull_request:
    branches:
    - main
    paths-ignore:
    - .editorconfig
    - .gitignore
    - .gitattributes
    - LICENSE
    - '**/*.md'
jobs:
  build:
    permissions:
      actions: write
      contents: read
      id-token: write
      packages: write
    runs-on: ubuntu-latest
    env:
      GRADLE_OPTS: -Dorg.gradle.console=plain -Dorg.gradle.logging.stacktrace=all
        -Dorg.gradle.java.installations.auto-download=false
    steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: 0
    - name: Setup JDK
      uses: actions/setup-java@v5
      with:
        java-version: 17
        distribution: temurin
        cache: gradle
    - name: Validation Gradle Wrapper
      uses: gradle/actions/wrapper-validation@v5
    - name: Building
      run: ./gradlew clean build
    - name: Publishing
      if: github.ref == 'refs/heads/develop'
      env:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
      run: ./gradlew publishMavenPublicationToGithubRepository
    - name: DeepSource
      if: github.ref == 'refs/heads/develop'
      env:
        DEEPSOURCE_DSN: "${{ secrets.DEEPSOURCE_DSN }}"
      uses: deepsourcelabs/test-coverage-action@master
      with:
        key: java
        coverage-file: report-aggregation/build/reports/jacoco/testCodeCoverageReport/testCodeCoverageReport.xml
        dsn: "${{ secrets.DEEPSOURCE_DSN }}"
    - name: Deleting old workflow runs
      uses: Mattraks/delete-workflow-runs@v2
      with:
        retain_days: 1
